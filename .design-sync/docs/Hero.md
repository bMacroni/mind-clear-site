---
category: Page Sections
---
Full-viewport opening section: the headline, the sub-line, the Google Play badge, and faint thought-fragments scattered in the margins.

Takes no props — the copy is fixed. It carries the approved primary headline ("Clear your head. One step at a time.") and must not be reworded without marketing review.

The scattered fragments are decorative (`aria-hidden`) and represent the swirling head the product resolves. The golden thread fades in at the bottom edge and runs continuously through every section placed below.

Always the first child of `MindClearPage`.
