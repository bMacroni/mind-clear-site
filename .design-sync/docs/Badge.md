---
category: Primitives
---
shadcn/ui badge. Unused on the live site.

**Read before using.** Variants resolve against the retired dark palette — `default` is electric gold `#FFD700`, `secondary` is deep slate. For a Golden Thread chip, override the colors, or use `TaskCardMockup`'s `pill` / `priorityPill` props, which already carry the right treatment.

Props: `variant` (`default` | `secondary` | `outline` | `destructive`) plus native `div` attributes.
